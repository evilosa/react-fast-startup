// @flow
export const ActionColors = {

};

export const createActionStyle = (colors = ActionColors) => ({
  colors,
});

export const ActionStyle = createActionStyle(ActionColors);

export default ActionStyle;